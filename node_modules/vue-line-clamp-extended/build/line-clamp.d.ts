import { VNode, VNodeDirective } from 'vue';
export interface LineClampDirectiveBinding extends VNodeDirective {
    value: {
        text: string;
        lines?: number;
        expanded?: any;
    };
}
export declare const lineClamp: {
    inserted: (el: HTMLElement, binding: LineClampDirectiveBinding, vnode: VNode) => void;
    update: (el: HTMLElement, binding: LineClampDirectiveBinding, vnode: VNode) => void;
};
